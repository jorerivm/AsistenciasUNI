<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asistenciasuni">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AsistenciasUNI">
        <activity android:name=".QRreader"></activity>
        <activity android:name=".QRcreator" />
        <activity android:name=".profesor" />
        <activity android:name=".ui.Olvido" />
        <activity
            android:name=".ui.login.AppLogin"
            android:label="@string/title_activity_app_login" />
        <activity android:name=".Entrar" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>